apiVersion: v1
kind: Service
metadata:
  name: springsvc
  labels:
    app: springapp
spec:
  type: NodePort
  selector:
    app: springapp
  ports:
    - port: 8080
      nodePort: 30011
      targetPort: 8080

